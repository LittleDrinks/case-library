# “强国有我”思政案例库

围绕教学案例的创作、审核、发布、检索和复用形成统一语言。

## 角色与界面

**作者模式**:
画布工作台的可编辑形态，面向案例作者。
**读者模式**:
画布工作台中阅读他人已发布案例的形态；正文只读，读者可与 AI 私人讨论，将来源加入自己的已有或新建案例，正文与聊天不自动复制。
**审核模式**:
画布工作台的只读形态，面向案例管理员；与作者模式共享正文渲染、批注和 AI，增加审核动作。
**案例**:
由一份案例正文、案例附件、批注和版本组成的内容单元。
**案例正文**:
案例中唯一的主文档，以 ProseMirror 文档 JSON 为真源，也是画布工作台的主工作面。
**案例附件**:
教师为单个案例收集的网页副本或上传文件；随案例存在，可用 `[显示文字](attachment:附件ID)` 插入正文，案例发布前不进入素材库。
**画布工作台**:
案例阅读、创作与审核的统一界面，由正文画布、浮动大纲和 AI、批注、附件辅助面板组成。
_Avoid_: 工作台第二版、第二版工作台、OnlyOffice 工作台
**辅助面板**:
承接正文相关操作的侧栏；只响应教师当前动作，不主动判断写作需求，不承担全库浏览与管理。
**资料区（缓冲区）**:
案例工作台右侧统一收集平台案例、素材、上传文件和采集网页的面板，保留来源类型。Agent 可按需读取有权访问的资料，新检索来源经教师确认后加入；加入来源不自动修改正文。案例发布时展示全部保留条目，未使用资料由教师删除，内容仍受来源权限约束。

## AI 与工具

**AI**:
画布工作台中的通用智能辅助面板，按教师请求调用资料工具并返回答案、写作候选或候选批注。
_Avoid_: 研究、Copilot
**案例对话**:
围绕单个案例的私人 AI 讨论，归发起讨论的教师所有；再次打开该案例可继续，不随案例投稿或发布。
**Skill**:
教师在工作台选用的教学任务知识包，包含任务指令与配套资源，依赖平台提供的工具执行。
**Skill 版本**:
管理员通过网页上传并发布的固定内容包；系统识别元数据，教师选择已发布 Skill，每次执行记录实际版本与内容哈希。
**平台检索（`search_corpus`）**:
按当前用户权限检索已发布案例、知识和案例附件。
**联网检索（`web_search`）**:
检索互联网公开资源并返回网页候选，不自动采集或加入案例。
**网页采集（`fetch_url`）**:
教师勾选网页采集并提交指定公开 URL 后，抓取当时的内容副本并作为当前案例附件保存。
**AI 写工具**:
agent 对案例本身的操作工具（提议修订、挂候选批注、标批注解决）；产出均需教师确认，不直接写入正文。
**写作候选**:
AI 返回的主方案或备选方案；教师明确选择替换、追加或新节后才写入正文。
**待确认修订**:
写作候选的内联形态。agent 写工具对正文单段的修改提议，以删除/新增高亮嵌在正文原位，附修改理由，教师接受或拒绝后才生效。
**修订阻塞**:
agent 积压三条未确认修订即暂停产出；教师逐条确认或点「继续生成」后恢复。
**批前快照**:
agent 开始一批修订前自动保存的版本快照，供一键回滚。
**DOCX 导出件**:
案例正文按固定版式生成的 Word 文件，不是编辑真源，也不参与运行时版本并发控制。
_Avoid_: DOCX 交换件、DOCX 导入件

## 批注体系

**批注**:
正文具体位置的反馈，锚定方式为「选区原文（quote）+ 所在小节」，来源包括 AI 审校、提交前自检、人工（审核员/作者）。
**选区锚定**:
在正文选中文字后浮出「+ 批注」，批注记录选区原文。
**模糊重挂**:
正文被改写后，按 quote 前缀渐进匹配重新定位批注；失败时展示原文快照并标「原文已变动」。
**批注线程**:
同一条批注下的追加回复序列（作者回应、审核员追问/确认），跨提交轮次保留。
**退回意见**:
审核动作「退回/要求补充」必填的原因说明，可附正文定位批注，不以已有批注作为退回前提。
**候选批注**:
AI 审校产出、尚未挂载的批注草稿；教师勾选并确认加入后生效。
**连续快标**:
作者在正文连续选区、逐条一句话挂批注的交互，全程不离开正文。
**批注任务**:
作者待处理批注可作为 agent 的工作来源；线程内「让 AI 解决」或批注面板批量交办，agent 逐条产出修订，处理后标解决。
**AI 自检**:
AI 对话中的一个动作：锁定当前版本快照、执行检查提示词，结果以批注呈现。编辑过程中不持续检查。
**补充证据**:
审核员通过批注明确提出的修改要求。编辑阶段不由系统主动判定。

## 版本与审核

**工作版本**:
作者正在编辑的案例内容，可与同一案例的公开发布版本并存。创建时可暂不填写完整标签，投稿前检查必填属性。
**投稿内容**:
教师确认保存到自己案例的正文、资料区与标签，包括已采用的教学设计、讨论题等成果；私人聊天与未采用建议不随投稿提交。
**投稿必填**:
标题、正文不能为空，管理员指定的每个必填标签组至少选择一个标签；不强制套固定章节。
**文档提取（extract）**:
教师先创建草稿，再在当前案例的 Agent 对话中提交 Word，提取可读取的文字、结构、图片和表格。保留已提取内容的字句与原章节，转换成系统可编辑格式，不自动润色、补写或套教学模板；正文修改经教师确认。原文件仅供任务临时处理，不长期保存或列为参考资料。
**图片资料**:
教师上传或从 Word 提取后保存到案例资料区的图片；正文使用同一图片资源，保留可还原的原位置，与其他资料一样参与来源管理。
**提取状态**:
处理中的条目显示灰色占位与旋转状态，提取并保存成功后恢复正常显示，失败显示叉号和原因；保留成功部分，标明无法提取的内容，部分完成不表示原文件已完整还原。
**工作版本修订号**:
服务器每次接受正文保存后递增的序号，用于拒绝旧标签页覆盖较新的工作版本。
**版本号**:
每次提交冻结生成的新版本（v1、v2…）；审核针对版本快照。
**冻结**:
提交后固定被审版本，作者需撤回后才能修改再提交，审核结论仅作用于该提交版本。
**撤回**:
审核结论产生前，作者收回提交并恢复工作版本编辑；原待审提交不再接受审核决定。
**发布版本**:
审核通过后直接公开的案例快照。作者可另开工作稿，旧发布版继续可读；新稿通过审核后替换默认展示版本，历史引用仍定位原已发布版本。
**版本对比（diff）**:
重新提交后，上一版与本版的文字级增删高亮。

## 引用体系

**引用案例**:
当前案例对另一个案例已发布版本的显式引用。
_Avoid_: 引用素材、引用案例最新状态
**事实依据**:
正文中的具体表述与本案例附件原文片段之间的关系。事实依据不指向其他案例。
**引用候选**:
AI 按教师需求检索、重排后交给教师判断的少量已发布案例。
**附件检索任务**:
教师主动委托 AI 查找平台或互联网资料；案例结果可引用为案例版本，素材与网页结果由教师选取后加入案例附件。
**〔n〕上标**:
正文中的引用标记，编号按正文出现顺序自动重排。
**参考资料区**:
Word 文末包含当前案例资料区全部保留来源的名称、出处、版本和平台链接，随资料增删更新，不自动附上来源全文；正文中的具体引用标记对应实际使用的来源，打开链接时重新检查阅读权限。
**来源移除**:
从当前案例资料区删除来源；正文已有引用时，先定位并移除或替换引用标记，再删除来源，不自动删除正文。
**来源下线**:
来源内容停止开放，已有引用保留名称、版本和下线状态；正常更新仍保留对原已发布版本的引用。
**引用卡**:
展示一个被引用案例版本，支持定位正文、查看版本和取消引用。
**局部引用关系图（ego 图）**:
以单个案例为中心的两跳案例引用关系图。
**检索挂载**:
检索页对多选结果执行「添加到我的案例」，按类型自动分流：案例挂为引用案例，素材与网页进案例附件，知识不可挂载。

## 附件与权限

**素材**:
已发布案例版本附件在素材库中的只读投影；条目名称公开，内容受资料访问级别控制，相同来源或内容只保留一条。
**素材入库**:
管理员批量导入资料或案例发布附件时先生成候选素材，审核通过后进入素材库。
**素材挂载**:
案例版本附件与聚合素材之间的来源关系。一个素材可挂载多个案例版本，新增挂载不覆盖聚合素材已有访问级别。
**原文副本**:
联网采集时保存的页面内容与来源信息，用于事实核验，不随外部网页变化。
**资料访问级别**:
公开内容人人可读，校内内容限已确认身份的校内账号，受限内容限资料所有者和管理员。已发布案例的全部资料条目可见，摘要、原文、预览和下载按内容权限开放；引用他人资料不改变其访问级别，权限由原资料管理者控制。
**知识**:
教材的结构化章节，供 AI 检索与理解，不作为引用案例。
**批量导入任务**:
管理员提交的一组资料处理作业；每个文件独立去重、解析并生成候选记录，单项失败不撤销其他成功项。

## 账号与运维
**正式账号**:
通过真实凭据登录并受角色、状态和强制改密约束的人员身份。
**演示账号**:
仅在显式 demo 或 test 环境创建的固定测试身份，不得出现在生产环境。
**平台导出包**:
用于迁移与灾备的加密数据包，包含业务数据、文件和校验清单，不包含密钥、会话或运行日志。

## 检索与图谱

**分类目录**:
管理员维护的标签组与标签，学科、课程、案例类型、思政元素均可按组组织；新增组或标签直接出现在案例属性与检索入口。层级用于组织和查找标签，不隐含标签间的筛选逻辑；关键词由教师自由填写。
**案例标签**:
案例与目录标签的关联，每个标签分别表示具有或不具有该属性；一个案例可选择多个标签，不受标签组限制。
**标签条件**:
以标签关联为判断依据的全部符合、任一符合或二者组合；标签所属目录不影响条件含义。普通页面默认全部符合，可切换任一符合；Agent 检索支持混合条件，前端高级条件编辑器不纳入当前范围。
**替代候选**:
Agent 自主放宽检索条件后找到的资源，明确标注未满足的原始要求；与严格匹配结果区分，不视为满足教师的必选条件。
**AI 检索摘要**:
非空检索成功且有可见结果后自动生成的平台资源解读，位于结果列表上方，默认展示部分内容，可展开全文并收起；普通结果列表保留，持续讨论进入案例工作台。
**搜索范围**:
搜索页检索平台案例、教材和素材；案例工作台按任务需要联网，外部来源明确标注，经教师选取后保存为案例附件。
**供给分布**:
按标签目录统计案例数量，默认采用当前发布版本；切换全部案例时，有工作稿采用最新工作内容，否则采用发布内容，每个案例只计一次。案例可计入多个标签，各标签数量不要求相加等于案例总数，没有案例的分类显示为零。
**检索目录**:
面向检索的案例、知识和素材只读集合；内容来自业务真源，可整体重建，不承载业务写入。
_Avoid_: 搜索数据库、业务副本
**分面计数**:
标签后的数字表示当前可见结果中具有该标签的数量，包含已选标签条件；任一符合模式下不代表加入该标签后的总数，当前计数为零的标签仍可用于扩大范围。
**案例检索**:
检索已发布案例及有权访问的附件内容，结果按案例聚合，不把附件作为独立结果实体。
**目录式列表**:
无关键词时浏览案例、知识和素材的统一列表，默认最新排序并分页。
**高级筛选下拉**:
页签行右侧的一行筛选下拉框（百度高级搜索式），下拉内 checkbox 多选，选中条件以 chip 行回显；维度按页签出项（ADR 0007）。

## 首页与推荐

**专题集**:
管理员从已发布案例中选编并排序的主题集合；同一案例可加入多个专题。
**动态卡**:
首页的三张卡片——平台增量动态 / 时政新闻流 / 平台公告。
**时政新闻流**:
经 web-search 抓取的思政/高教新闻列表，本轮只读跳原文，不入库。
**教学成果**:
教师通过 AI 或 Skill 形成的教学设计、讨论题、PPT 提纲等内容；确认保存到自己案例的正文或资料区后随该案例投稿，未采用结果保留在私人对话中。
