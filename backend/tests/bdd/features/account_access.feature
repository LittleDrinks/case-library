# language: zh-CN
功能: 账号与权限
  演示账号登录后按角色执行业务；未认证、未带CSRF、越权与并发旧版保存被拒绝

  场景: 教师登录成功并恢复会话
    假如 已登录的教师
    那么 会话中显示教师身份与csrf令牌

  场景: 错误密码被统一拒绝
    当 教师"小张"用错误密码登录
    那么 登录被拒绝且不泄露账号是否存在

  场景: 未登录无法查看他人草稿
    当 匿名访客请求案例"c-draft-1"
    那么 操作被拒绝，提示"案例不存在"

  场景: 缺少CSRF令牌无法创建案例
    假如 已登录的教师
    当 教师不带CSRF令牌创建案例"csrf缺失案例"
    那么 操作被拒绝，提示"CSRF token 无效"

  场景: 管理员不能编辑教师的草稿
    假如 教师创建草稿案例"权限边界案例"，正文包含"只有作者能改"
    当 管理员尝试把"权限边界案例"正文改为"管理员改写"
    那么 操作被拒绝，提示"无权编辑该案例"

  场景: 强制改密账号被拦截业务写入
    假如 名册账号"10000001"首次登录
    当 该账号尝试创建案例"改密前案例"
    那么 响应状态码为403
    并且 提示要求先修改密码

  场景: 停用账号无法登录
    当 停用账号"10000003"尝试登录
    那么 停用账号按无效凭据处理

  场景: 旧标签页保存被当前修订号拒绝
    假如 教师创建草稿案例"并发保存案例"，正文包含"初始正文"
    并且 另一标签页已把"并发保存案例"正文改为"较新正文"
    当 教师用旧修订号把"并发保存案例"正文改为"过期正文"
    那么 响应状态码为409
    并且 冲突响应带回当前修订号
