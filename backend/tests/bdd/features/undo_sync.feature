# language: zh-CN
功能: AI直接写入与撤销
  AI整篇写入保存为独立版本；教师撤销恢复原文且重复撤销幂等；正文再变化后撤销被拒绝

  背景:
    假如 已登录的教师

  场景: AI直接写入后教师撤销恢复原文
    假如 教师创建空白草稿案例"撤销案例"
    并且 教师在该案例的线程中有一条AI写入
    当 教师撤销该写入
    那么 正文恢复为写入前的内容且写入状态为已撤销

  场景: 重复撤销同一写入保持幂等
    假如 教师创建空白草稿案例"幂等案例"
    并且 教师在该案例的线程中有一条AI写入
    并且 教师在"幂等案例"已撤销一条AI写入
    当 教师再次撤销同一写入
    那么 写入状态仍为已撤销且修订号不再变化

  场景: 正文变化后撤销被拒绝
    假如 教师创建空白草稿案例"拒绝案例"
    并且 教师在该案例的线程中有一条AI写入
    当 教师把正文改为"撤销后的新编辑"并保存
    并且 教师撤销该写入
    那么 撤销被拒绝且正文与写入保持不变
