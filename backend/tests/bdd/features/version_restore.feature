# language: zh-CN
功能: 版本隔离与恢复
  正式版本互相隔离；恢复当前稿不破坏历史；手动命名版本与删除约束

  背景:
    假如 已登录的教师

  场景: 作者冻结历史版本后继续编辑互不影响
    假如 教师创建草稿案例"版本案例"，正文包含"第一稿正文"
    当 教师把"版本案例"正文改为"第二稿正文"
    并且 教师以"检查点一"命名当前版本
    并且 教师把"版本案例"正文改为"第三稿正文"
    那么 版本历史中"检查点一"保存的正文是"第二稿正文"
    并且 当前工作稿正文是"第三稿正文"

  场景: 恢复目标版本保留当前稿为基线
    假如 教师的"恢复案例"存在历史版本"检查点甲"且当前稿已改动
    当 教师恢复"检查点甲"
    那么 当前工作稿正文是历史版本"检查点甲"的内容
    并且 版本历史新增恢复记录且旧版本仍在

  场景: 工作稿修订号过期时恢复被拒绝且工作稿与历史不变
    假如 教师的"取消恢复案例"存在历史版本"检查点乙"且当前稿已改动
    当 教师以过期的修订号尝试恢复"检查点乙"
    那么 响应状态码为409
    并且 当前工作稿正文保持"取消恢复案例"的最新改动
    并且 版本历史保持不变

  # 确认对话框取消恢复由浏览器 BDD 验证零请求与状态不变。

  场景: 删除历史版本不影响当前稿
    假如 教师的"删除案例"存在手动命名版本"将被删除"
    当 教师删除版本"将被删除"
    那么 版本历史不再包含"将被删除"
    并且 当前工作稿正文不变

  场景: 被审核引用的历史版本不可删除
    假如 教师的"引用保护案例"已提交送审且审核中
    当 教师删除该送审版本
    那么 响应状态码为409
