# language: zh-CN
功能: 案例生命周期
  草稿投稿、审核、撤回、发布与新工作稿并存

  背景:
    假如 已登录的教师

  场景: 作者提交草稿进入待审并冻结编辑
    假如 教师创建草稿案例"投稿案例"，正文包含"准备提交的内容"
    当 教师提交"投稿案例"送审
    那么 提交成功且案例状态为"待审"
    并且 案例正文锁定为只读

  场景: 审核通过后案例发布并默认公开
    假如 教师"教师"创建的草稿案例"发布案例"已提交送审
    当 管理员开始审核并最终通过
    那么 案例状态为"已发布"且公开可见

  场景: 作者在审核开始后撤回回到工作稿
    假如 教师的草稿案例"撤回案例"已提交且审核已开始
    当 教师撤回"撤回案例"
    那么 案例回到草稿状态且正文可继续编辑

  场景: 审核退回必须写明原因
    假如 教师的草稿案例"退回案例"已提交且审核已开始
    当 管理员不带原因退回"退回案例"
    那么 校验失败并要求提供退回原因

  场景: 管理员带原因退回后作者看到反馈并重新提交
    假如 教师的草稿案例"补充案例"已提交且审核已开始
    当 管理员以"请补充教学目标"退回"补充案例"
    并且 教师看到"补充案例"的退回原因"请补充教学目标"
    并且 教师重新提交"补充案例"
    那么 案例状态为"待审"且历史反馈被清除

  场景: 未另起工作稿时不能直接覆盖发布内容
    假如 教师的草稿案例"锁定案例"已发布
    当 教师尝试把"锁定案例"正文改为"发布后改动"
    那么 操作被拒绝，提示"案例当前不可编辑"

  场景: 管理员下线已发布案例后作者可另起新稿
    假如 教师的草稿案例"下线案例"已发布
    当 管理员下线"下线案例"
    并且 教师对"下线案例"执行另起新稿
    那么 案例回到草稿状态且已发布版本历史仍被记录

  场景: 作者另开工作稿时旧发布版保持公开且新稿通过后替换默认版本
    假如 教师的草稿案例"并存案例"已发布
    当 教师对"并存案例"执行另起新稿
    并且 教师把"并存案例"正文改为"第二版正文"
    那么 匿名读者仍看到原发布版本
    当 教师提交"并存案例"送审
    那么 提交成功且案例状态为"待审"
    并且 匿名读者仍看到原发布版本
    当 管理员开始审核并最终通过
    那么 默认发布正文是"第二版正文"且原发布版本仍可读取
