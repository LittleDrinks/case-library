# language: zh-CN
功能: 标签与Skill平台
  案例保存标签并按必填组约束投稿；管理员上传发布Skill，教师读取已发布目录

  背景:
    假如 已登录的教师

  场景: 案例保存标签并去重
    假如 教师创建草稿案例"标签案例"，正文包含"标签正文"
    当 教师保存标签"tag-seed-4-1"与"tag-seed-4-2"
    那么 案例标签去重后为两个不同的种子标签

  场景: 必填标签组缺失时投稿被拦截
    假如 教师创建草稿案例"标签约束案例"，正文包含"约束正文"
    并且 管理员已建必填组"投稿必填组"含标签"教学目标"
    当 教师提交"标签约束案例"送审
    那么 提交被拦截并提示缺少必填组
    当 教师补选必填组标签并再次提交
    那么 操作成功

  场景: 管理员上传并发布Skill包
    假如 已登录的管理员
    当 管理员上传Skill压缩包
    并且 管理员发布该Skill版本
    那么 教师端目录可见该Skill并可读取SKILL.md内容

  场景: 教师不能上传或发布Skill
    当 教师上传Skill压缩包
    那么 响应状态码为403
