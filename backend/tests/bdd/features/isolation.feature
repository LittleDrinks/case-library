# language: zh-CN
功能: 隔离与越权
  线程、案例与用户边界不可穿越：跨线程决策、跨用户读写、重复绑定均被拒绝

  背景:
    假如 已登录的教师

  场景: 其他教师的AI线程不可见
    假如 教师创建草稿案例"隔离案例"，正文包含"隔离正文"
    当 另一位教师尝试在"隔离案例"创建AI线程
    那么 响应状态码为404
    当 教师列出自己的AI线程
    那么 线程列表只包含教师自己的线程

  场景: 绑定到错误线程的修订候选被拒绝
    假如 教师创建草稿案例"边界案例"，正文包含"边界正文"
    并且 教师在该案例的线程甲中有一条待确认修订
    当 教师通过线程乙提交该修订的采用
    那么 响应状态码为404
    并且 修订候选仍为待确认状态

  场景: 修订候选被拒绝后正文不变
    假如 教师创建草稿案例"拒绝案例"，正文包含"原始正文"
    并且 教师在该案例的线程中有待确认修订"改写正文"
    当 教师拒绝该修订
    那么 案例正文保持"原始正文"且修订标记为已拒绝

  场景: 管理员看不到教师的私人讨论
    假如 教师创建草稿案例"私人讨论案例"，正文包含"私人正文"
    并且 教师在该案例的AI线程已有一轮回答
    当 管理员请求该案例的AI线程
    那么 管理员请求被拒绝且看不到教师线程
