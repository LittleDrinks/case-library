# language: zh-CN
功能: AI线程生命周期
  对话线程的断线恢复、显式停止与失败重试

  背景:
    假如 已登录的教师

  场景: 教师提问获得AI回答
    假如 教师创建草稿案例"对话案例"，正文包含"讨论正文"
    当 教师在AI线程提问"帮我分析这个案例"
    那么 AI回答包含"模拟回答"

  场景: 断线后重新打开线程恢复历史消息
    假如 教师创建草稿案例"对话案例"，正文包含"讨论正文"
    假如 教师在"对话案例"的AI线程已有一轮回答
    当 教师重新打开AI线程
    那么 线程快照包含此前的用户消息与AI回答

  场景: 教师显式停止运行中的回答
    假如 教师创建草稿案例"停止案例"，正文包含"停止正文"
    当 AI正在回答时教师点击停止
    那么 该次运行结束于"cancelled"且无残留活动运行

  场景: 回答失败后教师重试得到新运行
    假如 教师的提问在"重试案例"中因服务异常失败
    当 教师重试该条消息
    那么 新的运行成功完成且引用原用户消息
